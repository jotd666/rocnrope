- ECS version: don't update 2 buffers at once, just use a counter with 2,1,0
  and use current drawn buffer only (works with async refresh mode, which should be a standard
  in all games where tiles are animated: GnG!)
- tip of grappling hook missing but only at level 3

- set DSW difficulty lives, etc.
- highscore load/save
 
- demo version left bottom dino doesn't get angry and climbs at once: sequence disrupted

 
converter misses the optimization:
	GET_ADDRESS	0x818a                     	| [$61b6: std    $818a]
	move.w	d1,(a0) |  [...]
	MAKE_D                                 	| [...]


skip intro:
bpset D5A2,,{PC=$d627;g}
bpset D64A,,{PC=$d6a2;g}




